<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>content blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
  <style>
    body {
      background-color: #f8f9fa;
      padding: 20px;
    }

    .container {
      max-width: 900px;
      margin: auto;
      background-color: #ffffff; /* 배경색 추가 */
      padding: 30px; /* 패딩 추가 */
      border-radius: 15px; /* 둥근 테두리 추가 */
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1); /* 그림자 추가 */
    }

    .card {
      width: 100%;
      margin-top: 20px;
      border: 1px solid #dee2e6;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .card-body {
      background-color: #fff;
      padding: 20px;
    }

    .card-title {
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 15px;
    }

    .card-subtitle {
      font-size: 14px;
      color: #6c757d;
      margin-bottom: 10px;
    }

    .card-text {
      font-size: 18px;
      line-height: 1.6;
      margin-bottom: 15px;
    }

    .card img {
      max-width: 100%;
      height: auto;
      margin-bottom: 15px;
      border-radius: 8px;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
    }

    .card-link {
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
      display: inline-block;
      margin-top: 10px;
    }

    .card-link:hover {
      text-decoration: underline;
    }
    .bg-image {
      max-width: 40%; /* 배경 이미지 최대 가로 넓이 100%로 설정 */
      height: auto; /* 세로는 자동으로 조절 */
      margin: 0 auto; 
      display: block;
    }
  </style>
</head>
<body>
  <%- include('menu.html') %>

  <div class="container">
     <!-- 배경 이미지 추가 -->
    <img src="/image/write.png" alt="배경 이미지" class="bg-image">
     <!-- 카드 시작 -->
    <div class="card">
      <div class="card-body">
        <div class="container">
          
          <img src="/image/pencil2.svg"alt="text Icon" width="50" height="50">
        <!-- 카드 제목 -->
          <h5 class="card-title"><%= data.title %></h5>
           <!-- 카드 작성일 -->
        <h6 class="card-subtitle mb-2 text-body-secondary"><%= data.date %></h6>
         <!-- 카드 내용 -->
        <p class="card-text"><%= data.content %></p> 
          <!-- 작성자 정보 -->
        <h6 class="card-subtitle mb-2 text-body-secondary"><%= data.userid %></h6>
        <img src="<%= data.path %>" width= "200"/> <p></p>
        <!-- 블로그 글 수정하기 링크 -->
        <a href="/edit/<%= data._id %>" class="card-link">블로그 글 수정하기</a>     
        <a href="/listmongo" class="card-link">목록</a>     
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>
</html>
